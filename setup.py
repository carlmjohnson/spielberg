#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='Spielberg',
    version='0.0.1',
    packages=find_packages(),
    package_dir={},
    classifiers=[
    ],
    include_package_data=True,
    zip_safe=False)
